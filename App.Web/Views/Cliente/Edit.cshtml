@model App.Web.Models.ClienteModel

@using (Html.BeginForm("Edit", "Cliente", FormMethod.Post, new { id = "formValidate" }))
{
    @Html.Hidden("Id", Model.Id)
    <center>
        <h2>Editar Cliente</h2>
    </center>
    <hr style="background-color: #5e35b1; height: 2px">
    @Html.Partial("_CreateOrEdit")

}